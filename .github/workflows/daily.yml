      - name: Show this week's member link
        run: |
          LINK="https://kfxcode.github.io/mlb-picks/p/${{ steps.rotate.outputs.token }}/"
          {
            echo "## This week's Cover1Picks member link"
            echo ""
            echo "**$LINK**"
            echo ""
            echo "ISO week \`${{ steps.rotate.outputs.week }}\`. Paste this into your WHOP"
            echo "product content (the Link app) so only active members can open it."
            echo "It stops working when the token rotates next Monday."
            echo ""
            echo "The daily picks also post automatically into your Whop forum, so"
            echo "this link is the optional full board -- not the only way in."
          } >> "$GITHUB_STEP_SUMMARY"
          echo "MEMBER LINK: $
